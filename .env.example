PORT=2026
VALIDA_CONFIG=valida.json
# Standalone: SQLite, local execution, no Redis.
DATABASE_URL=file:./data/valida.db
# Execution deadline defaults to one hour. Set 0 to disable it.
# RUN_TIMEOUT_MS=120000
# Distributed: PostgreSQL and Redis. Start API and worker processes separately.
# DATABASE_URL=postgres://valida:valida@localhost:5432/valida
# REDIS_URL=redis://localhost:6379
